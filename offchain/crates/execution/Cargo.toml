[package]
name = "execution"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
tracing = { workspace = true }

# Internal crates
types = { path = "../types" }
merkle = { path = "../merkle" }
